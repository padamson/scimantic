[project]
name = "scimantic-monorepo"
version = "0.1.0"
description = "Scimantic Monorepo: Semantic Research Framework & VS Code Extension"
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["scimantic-core"]

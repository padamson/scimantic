[project]
name = "semflow-monorepo"
version = "0.1.0"
description = "SemFlow Monorepo: Semantic Research Framework & VS Code Extension"
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["semflow-core"]
